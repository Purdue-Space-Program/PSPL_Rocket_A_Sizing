# Fluids/Combustion
CEA_Wrap == 2.0.0
# genericf2py; sys.platform == 'darwin' # only needed if on mac (darwin means MacOS for some reason lol) # cant get rocketcea to work on mac :(

# Math/Data
numpy
scipy
tqdm
pandas

# Plotting
matplotlib
# plotly

# GUI
# nicegui
# pyqtgraph
# pyqt5

# why u fail :(
CoolProp # new version works? #== 6.8 # newer version errors when debugging